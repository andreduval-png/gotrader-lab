# IFVG V3/V4 Impact

Frozen parameter values are unchanged. V4 remains research-only and still adds only its shallow-retest-depth condition over v3.

Classification: `BUG_FIX_SEMANTICS_CHANGED` because historical scoring no longer assumes an unobserved midpoint fill at decision time. Price geometry is unchanged, but trade inclusion and entry timestamps can change. The shared producer lifecycle applies equally to v3 and v4.

Neither profile is promoted. Corrected historical evidence must be rebuilt before old performance claims can validate this lifecycle.

