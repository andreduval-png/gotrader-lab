# BT1.6 Restart And Resume Report

Status: `PENDING_LIVE_CONTROLLED_INTERRUPTION`

BT1.6 requires the existing runner's `--interrupt-after-pages` boundary and
expected exit code 75. The resumed full report must have action `resumed`, bind
the same request, verify the repository, and contain no duplicate committed
partitions. Manual process termination is not accepted as the primary proof.
