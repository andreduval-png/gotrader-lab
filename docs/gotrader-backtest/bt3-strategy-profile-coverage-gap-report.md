# BT3 Strategy Profile Coverage Gap Report

Date: 2026-08-13

## Scope

This report classifies the 27 entries in the authoritative strategy baseline manifest against the accepted BT3 canonical fixture and parity lifecycle. It does not authorize runtime adoption, promotion, Paper Demo, broker contact, or execution.

## Classification

### Canonically covered strategy families

- IFVG: `ifvg_v1`, `ifvg_filtered_v2_research`, `ifvg_fresh_retest_v3_research`, and `ifvg_fresh_retest_v4_candidate`.
- CMD: `ict_cmd_short_paper_watchlist_v1` and `cmd_high_displacement_v2_research`.
- Silver Bullet: `silver_bullet_v1` and `silver_bullet_v2_refined_research`.
- Turtle Soup: `turtle_soup_v1`.
- CISD: `cisd_v1`.
- Session Raid: `nasdaq_london_raid_ny_reversal_v1` and `nasdaq_london_raid_ny_reversal_v2_filtered_research`.

These families already have canonical adapters, compact generated fixtures, parity tests, native geometry preservation, and authority-none contracts.

### Missing canonical coverage

- `ict-bread-and-butter-buy`.
- `ict-bread-and-butter-sell`.
- `ict-one-shot-one-kill`.

These Phase 2 profiles have deterministic legacy detectors and focused replay tests, but no BT3 canonical adapter, compact fixture generator, or parity lifecycle. This is the next implementation slice.

### Confluence-only models

- `grinch_reversal_expansion_confirmation_v1`.
- `grinch_model_1_research_v1`.
- `grinch_consolidation_research_v1`.

The Grinch models are deterministic confluence inputs only. They must not emit standalone canonical opportunities, trade plans, promotion evidence, or execution intent. Their existing legacy evidence is preserved until a separately authorized confluence-fact contract is introduced.

### Diagnostic or recognition-only entries

- `ict-order-block-taxonomy`.
- `universal_recognition_service`.
- `pd_array_setup_research_v1`.
- `scalp_setup_research_v1`.
- `market_map_only_diagnostic_v1`.
- `amd_power_of_three_research_v1`.

These entries provide context, taxonomy, or recognition. They are not independently promotable and must not own canonical entry, stop, target, or execution geometry.

### Catalog-only placeholders

- `camerons_model_research_v1`.
- `crt_research_v1`.
- `ote_research_v1`.

These remain non-executable placeholders until a future slice explicitly defines and validates deterministic detectors.

## Decision

The first remaining BT3 strategy profile coverage slice is the Phase 2 canonical adapter lifecycle. It must preserve detector-owned direction and geometry exactly, fail closed when geometry or approved-profile evidence is incomplete, serialize no raw candles, and retain authority `none/none/none`.

