# INT-3A.1 Static Winner and Geometry Scan

Authoritative multi-candidate paths contain no score, R:R, first, last, or
max-reduce winner. Canonical ordering is stable and non-authoritative.

Classified matches:

- `ictIfvg.ts` `candidates[0]`: nearest target-pool selection inside one IFVG
  producer, not cross-strategy candidate selection.
- Current Opportunity confidence sort: legacy/noncanonical presentation rank;
  canonical actionability and singular selection come only from the unified
  set.
- Phase 2 confidence sorts: source-blocked detector-local research ranking.
- Current Read and Activate Market fallbacks: guarded compatibility for packets
  that lack the new disposition field; current canonical packets cannot enter
  them during conflict.

G2.2 geometry acceptance reports zero downstream actionable constructors.
No cross-strategy geometry merge, downstream reconstruction, stop widening,
target stretching, or current-price geometry fallback was introduced.

