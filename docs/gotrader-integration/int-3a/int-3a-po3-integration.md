# INT-3A Power Of Three Integration

`ict_power_of_three_v1` owns the canonical accumulation, manipulation, and
distribution state sequence. It consumes canonical dealing-range, liquidity,
displacement, and MSS facts without private redetection.

Terminal HOD/LOD objective precedence remains unresolved in accepted source
evidence. The runtime therefore emits state and context, then records
`PO3_TARGET_PRECEDENCE_SOURCE_BLOCKED`. It emits no geometry, signal plan, or
BT2 request.

The older `amd_power_of_three_research_v1` label is superseded and is not a
second executable strategy.
